'use client';
import React from 'react';
// import { globalStyles } from '../shared/styles';

export function Emotion({ children }) {
  return (
    <>
      {/* {globalStyles} */}
      {children}
    </>
  );
}
